# Sessão 2026-07-14

## O que foi alterado
- O roteamento do frontend foi ajustado para respeitar o subpath `/EBD/` no GitHub Pages.
- A navegação cliente passou a manter a base correta ao montar links e redirects.

## Conhecimento consolidado
- As rotas cliente devem ser montadas com `APP_BASE_PATH` derivado de `APP_BASE_URL`.
- Redirects e `history.pushState` precisam ser base-aware.

## Próximos passos
- Manter a navegação consistente em todas as páginas novas.
